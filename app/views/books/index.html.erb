<h1>Books</h1>

<table class="table table-hover">
  <tr>
    <th>Rank</th>
    <th>Book</th>
    <th>Author</th>
    <th>Description</th>
    <th>Upvote</th>



  </tr>

  <% @books.each do |book|%>
  <tr>

    <td><%= book.votes %></td>
    <td><%= link_to(book.name, books_show_path(book.id))  %></td>
    <td><%= book.creator %></td>
    <td><%= book.description %></td>
    <td><%= button_to "upvote", books_upvote_path(book.id), {class: 'btn btn-info'} %></td>

  </tr>

  <% end %>

</table>
