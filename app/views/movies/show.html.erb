<h1><%= @movie.name %></h1>
<h4> <%= @movie.creator %> </h4>

<p><%= @movie.description %></p>

<div class="buttons-show">
  <%= button_to "Edit #{@movie.name}", "/movies/#{@movie.id}/edit", class: 'btn btn-info' %>

  <%= button_to "Delete", delete_movie_path, method: :delete, class: 'btn btn-danger' %>
</div>
