<h1>Movies</h1>

<table class="table table-hover">
  <tr>
    <th>Rank</th>
    <th>Movie</th>
    <th>Author</th>
    <th>Description</th>
    <th>Upvote</th>



  </tr>

  <% @movies.each do |movie|%>
  <tr>

    <td><%= movie.votes %></td>
    <td><%= link_to(movie.name, movies_show_path(movie.id))  %></td>
    <td><%= movie.creator %></td>
    <td><%= movie.description %></td>
    <td><%= button_to "upvote", movies_upvote_path(movie.id), {class: 'btn btn-info'} %></td>

  </tr>

  <% end %>

</table>
