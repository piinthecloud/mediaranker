<h1>Albums</h1>

<table class="table table-hover">
  <tr>
    <th>Rank</th>
    <th>Album</th>
    <th>Author</th>
    <th>Description</th>
    <th>Upvote</th>



  </tr>

  <% @albums.each do |album|%>
  <tr>

    <td><%= album.votes %></td>
    <td><%= link_to(album.name, albums_show_path(album.id))  %></td>
    <td><%= album.creator %></td>
    <td><%= album.description %></td>
    <td><%= button_to "upvote", albums_upvote_path(album.id), {class: 'btn btn-info'} %></td>

  </tr>

  <% end %>

</table>
